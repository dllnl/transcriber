services:
  web:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      # Persist database and uploads
      - ./backend/instance:/app/instance
    env_file:
      - .env
    restart: always
